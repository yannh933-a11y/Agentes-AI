import Link from 'next/link';
import { agentes } from '../../lib/agentes';
import { Badge, ButtonLink, Card, Container } from '../components/ui';
export const metadata={title:'Catálogo de agentes | OpenClaw'};
export default function AgentesPage(){ return <main className="pt-28 pb-20"><Container><div className="text-center max-w-3xl mx-auto mb-12"><Badge>Catálogo de agentes</Badge><h1 className="mt-4 text-4xl sm:text-6xl font-black tracking-tight">Escolha o agente ideal para sua empresa.</h1><p className="mt-5 text-slate-400 text-lg">Cada agente tem uma base padrão e depois é personalizado com dados, regras e objetivos da empresa contratante.</p></div><div className="grid md:grid-cols-2 lg:grid-cols-4 gap-5">{agentes.map(a=><Link href={`/agentes/${a.slug}`} key={a.slug} className="card rounded-3xl p-6 no-underline"><div className="flex justify-between items-start mb-5"><div className="w-12 h-12 rounded-2xl bg-red-500/10 border border-red-500/20 flex items-center justify-center font-black text-red-200">{a.icon}</div>{a.destaque&&<span className="text-[10px] uppercase tracking-widest text-red-200 font-black">Popular</span>}</div><h2 className="font-black text-xl mb-2">{a.nome}</h2><p className="text-slate-400 text-sm leading-relaxed min-h-[88px]">{a.descricao}</p><div className="mt-5 flex items-end gap-1"><span className="text-3xl font-black">R$ {a.preco}</span><span className="text-slate-500 text-sm mb-1">/mês</span></div><div className="mt-5 text-red-300 font-black">Ver detalhes →</div></Link>)}</div><div className="mt-12 text-center"><ButtonLink href="/pre-cadastro">Solicitar agente personalizado</ButtonLink></div></Container></main> }
