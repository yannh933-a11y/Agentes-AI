import { agentes } from '../../lib/agentes';import { Card, Container } from '../components/ui';export default function Page(){return <main className="pt-28 pb-20"><Container><h1 className="text-5xl font-black mb-8">Meus agentes</h1><div className="grid md:grid-cols-3 gap-5">{agentes.slice(0,3).map(a=><Card key={a.slug}><h2 className="font-black text-xl">{a.nome}</h2><p className="text-slate-400 mt-2">Status: configurado para empresa demo.</p></Card>)}</div></Container></main>}
