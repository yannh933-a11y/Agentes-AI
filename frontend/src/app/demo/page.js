import DemoPageClient from './DemoPageClient';

export const metadata = {
  title: 'Demo de agente de IA | Agentes AI',
  description: 'Veja uma demonstração comercial de como agentes de IA conduzem atendimento, vendas, suporte, agendamento e cobrança.',
};

export default function DemoPage() {
  return <DemoPageClient />;
}
